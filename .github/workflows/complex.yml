Name: complex
On : [Push]
Jobs:
	Job1:
	 runs-on: ubuntu-latest
   steps:
     -run:date
	Job2:
	  runs-on: windows-latest
    steps:
     -run:date
	Job3:
	  runs-on:macos-latest
    steps:
     -run:date
	Job4:
	  runs-on:ubuntu-latest
    steps:
     -run:date
	  Needs: [job1,job2,job3]
